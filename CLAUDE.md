# Workspace Instructions

## Committing

Commit at logical checkpoints before proceeding to the next step. Do not batch all changes into a single commit at the end of a task. Each commit should represent one coherent, working change (e.g. migration + model together, frontend changes together, a config change on its own if it stands alone).
